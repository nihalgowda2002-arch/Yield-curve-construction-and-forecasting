# Yield Curve Construction & Forecasting -- dependencies
#
# The library is deliberately self-contained: the Kalman filter, VAR, ARIMA,
# Dynamic Nelson-Siegel and LSTM are all implemented from scratch on top of
# NumPy/SciPy, so no statsmodels / PyTorch / TensorFlow / QuantLib are needed.
#
# Floors below are the minimum versions the code relies on. It was developed
# and tested against the pinned versions shown in the trailing comments.

numpy>=1.24          # tested 2.4.4   (NumPy 2.x scalar semantics handled)
pandas>=2.0          # tested 3.0.2
scipy>=1.10          # tested 1.17.1  (optimize.least_squares, interpolate.PchipInterpolator, optimize.brentq)
matplotlib>=3.7      # tested 3.10.8
scikit-learn>=1.3    # tested 1.8.0   (MLPRegressor, GradientBoostingRegressor, StandardScaler)
